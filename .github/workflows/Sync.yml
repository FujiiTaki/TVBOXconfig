name: Sync Fork

on:
  schedule:
    - cron: '0 0 0/1 * * ? *' # everyday
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v1.9.0
        with:
          owner: gaotianliuyun
          base: master
          head: master
